# wf docs

Documentation site for [wf-cli](https://github.com/sagoez/workflow), built with [Fumadocs](https://fumadocs.dev).

```bash
pnpm install
pnpm dev        # localhost:3000
pnpm build      # static export
```
